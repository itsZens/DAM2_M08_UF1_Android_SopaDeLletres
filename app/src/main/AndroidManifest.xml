<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.fje.dam2.m08_uf1_android_sopadelletres">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AfterGameScoreActivity"
            android:label="@string/afterGameScore_label" />
        <activity
            android:name=".InstructionsActivity"
            android:label="@string/instruccions" />
        <activity
            android:name=".ScoreboardsActivity"
            android:label="@string/puntuacions" />
        <activity
            android:name=".PlayActivity"
            android:label="@string/jugar" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>